---
- name: Executar script Python localmente
  hosts: localhost
  connection: local
  tasks:
    - name: Executar o script Python
      command: python3 main.py
      args:
        chdir: "{{ backup_script_path }}"

